{
  "buildCommand": "cd web-ui/frontend && npm install && npm run build",
  "outputDirectory": "web-ui/frontend/dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
