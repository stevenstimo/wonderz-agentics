"""
Client Layer & Platform Integrations (CREW INTELLIGENT Spec v1.0).

Agents never talk directly to external APIs — always via UnifiedToolBridge.
Credentials are encrypted at rest via CredentialStore.
"""
